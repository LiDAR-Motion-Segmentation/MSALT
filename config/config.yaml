defaults:
  - salt_setup: husky_setup  
  - models: default
  - _self_

app:
  window_title: "SALT: Sensor Fusion Annotator"
  window_size: [1600, 900]

output:
  dir: "${hydra:runtime.cwd}/SALT_outputs_annotations"